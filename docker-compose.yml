services:
  mongodb:
    image: mongo:latest
    container_name: tcg-mongo
    ports:
      - "27018:27018"
    environment:
      MONGO_INITDB_DATABASE: tcg-collection
    command: ["mongod", "--port", "27018"]
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
    driver: local
